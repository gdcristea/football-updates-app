/**
 * Holds all the available routes
 */
export class RoutingPathsService {
  /**
   * Route paths mapping
   *
   * @type {{
   * leagueInformation: string,
   * teamResults: string}}
   */
  static paths = {
    leagueInformation: 'league-information',
    teamResults: 'team-results'
  };
}
